version: '3.8'

services:
  weather-monitor:
    build: .
    container_name: weather-monitor
    restart: unless-stopped
    env_file:
      - .env.prod

    volumes:
      - ./logs:/app/logs
      - /tmp:/run/sockets
      - /run/monitors/weather:/run/data