version: '3.8'

services:
  weather-monitor:
    build: .
    container_name: weather-monitor
    restart: unless-stopped
    # privileged: true
    env_file:
      - .env.prod
    volumes:
      - ./logs:/app/logs
      - /run/monitors/weather:/run/data
      - /tmp:/tmp